// test configuration constants, adapt to your environment!

const kUsername = "testuser"                    // local username
    , kPassword = "password"                    // local password (eg local IndexedDB)
    , kServiceUrl = "http://127.0.0.1:8080"     // service provider URL (datavaccinator vault access)
    , kServiceProviderId = 1                    // service provider ID (must match datavaccinator vault)
    , kServiceProviderPwd = "dv123";            // service provider password (must match datavaccinator vault)